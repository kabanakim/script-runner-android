name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build APK with Buildozer
      uses: ArtemSBulgakov/buildozer-action@v1
      id: buildozer
      with:
        command: buildozer android debug
        buildozer_version: stable

    - name: Upload APK
      uses: actions/upload-artifact@v2
      with:
        name: ScriptRunner-APK
        path: ${{ steps.buildozer.outputs.filename }}
```

4. Нажмите **"Commit new file"** внизу

---

### Способ 2: Визуально (что происходит)

GitHub увидит такую структуру:
```
📁 Ваш репозиторий
  ├── 📄 main.py
  ├── 📄 buildozer.spec
  └── 📁 .github
      └── 📁 workflows
          └── 📄 build.yml  ← этот файл
